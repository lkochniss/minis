# Minis Repository (Archived)

Dieses Repository ist **archiviert**. Die Daten wurden in das `Brain` Repository migriert.

**Bitte keine Änderungen mehr hier vornehmen.**

Die Bild-Assets werden weiterhin von diesem Repository aus bereitgestellt (via GitHub Raw URLs), daher darf das Repository **nicht gelöscht** werden.
